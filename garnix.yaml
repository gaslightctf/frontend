actions:
  - on: push
    run: deploy

builds:
  - include:
    - "packages.x86_64-linux.default"
    - "packages.x86_64-linux.prod"
    - "devShell.x86_64-linux.*"
    - "devShell.aarch64-darwin.*"

